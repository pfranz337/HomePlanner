<Window x:Class="HPAdmin.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:viewModels="clr-namespace:HPAdmin.UI.ViewModels"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:MainWindowViewModel}"
        Title="{Binding Title}" Height="350" Width="525">

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <prism:InvokeCommandAction Command="{Binding ViewLoadedCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Navigace -->
            <RowDefinition Height="*" />
            <!-- Obsah -->
        </Grid.RowDefinitions>

        <!-- Horní navigační panel pomocí Grid -->
        <Grid Grid.Row="0" Background="#EEE" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Úkoly" Command="{Binding ShowTasksCommand}" Margin="10" />
            <Button Grid.Column="1" Content="Přihlášení" Command="{Binding ShowLoginCommand}" Margin="10" />
            <Button Grid.Column="2" Content="Odhlásit" Command="{Binding LogoutCommand}" Margin="10" />
        </Grid>

        <!-- todo: tady by se to melo resit pomoci datatemptate a ne pres DIHelper -->
        <!-- Obsahová část -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentView}" />
    </Grid>

</Window>